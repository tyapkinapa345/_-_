docker compose version

docker --version

docker compose up

docker compose logs web

docker compose exec web python manage.py migrate

docker compose exec web python manage.py createsuperuser

docker compose exec web python manage.py shell

docker compose down



docker exec -it sales_mongo mongosh
use sales  # или имя вашей БД
db.sales.insertMany([
  { productName: "ASUS Vivobook", category: "Ноутбуки", quantity: 2, price: 50000, totalAmount: 100000, saleDate: new Date(2025, 2, 15) },
  { productName: "iPhone 15", category: "Смартфоны", quantity: 1, price: 80000, totalAmount: 80000, saleDate: new Date(2025, 2, 20) }
]);

http://localhost:5000/api/stats/monthly?year=2025&month=3



services:
  postgres:
    image: postgres:16
    container_name: postgres16
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: post1616!
      POSTGRES_DB: postgres
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_MULTIPLE_DATABASES: superstore
      POSTGRES_ADMIN_USER: admin
      POSTGRES_ADMIN_PASSWORD: admin1617!
    ports:
      - "5432:5432"
    volumes:
      - postgres_:/var/lib/postgresql/data
      - ./postgresql.conf:/etc/postgresql/postgresql.conf
      - ./pg_hba.conf:/etc/postgresql/pg_hba.conf
      - ./init-scripts:/docker-entrypoint-initdb.d
    command: postgres -c config_file=/etc/postgresql/postgresql.conf
    networks:
      - postgres_network
  
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin1617!
    ports:
      - "80:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - postgres_network

networks:
  postgres_network:
    driver: bridge

volumes:
  postgres_:
  pgadmin_data:

#Для подключения через DBeaver:

#Host: localhost
#Port: 5432
#Database: superstore
#Username: admin
#Password: admin1617!

#if need ip for pgadmin
# sudo docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' postgres16



# Подключение под postgres
# sudo docker compose exec postgres psql -U postgres -c "\du"
# sudo docker compose exec postgres psql -U postgres -c "\l"

# Проверка подключения под admin
# sudo docker compose exec postgres psql -U admin -d superstore


# Удаляем старые контейнеры если есть
# sudo docker compose down -v


# Запускаем
# sudo docker compose up -d



# Статус контейнера
# sudo docker compose ps

# Проверка логов
# sudo docker compose logs

# Проверка пользователей и баз
# sudo docker compose exec postgres psql -U postgres -c "\du"
# sudo docker compose exec postgres psql -U postgres -c "\l"

# Остановка
# sudo docker compose stop

# Запуск
# sudo docker compose start

# Перезапуск
# sudo docker compose restart

# Удаление с сохранением данных
# sudo docker compose down

# Удаление с удалением данных
# sudo docker compose down -v





dev@dev-vm:~/Downloads/dba/rel/postgresql$ sudo docker compose ps
NAME         IMAGE                   COMMAND                  SERVICE    CREATED         STATUS         PORTS
pgadmin      dpage/pgadmin4:latest   "/entrypoint.sh"         pgadmin    6 minutes ago   Up 6 minutes   0.0.0.0:80->80/tcp, [::]:80->80/tcp, 443/tcp
postgres16   postgres:16             "docker-entrypoint.s…"   postgres   6 minutes ago   Up 6 minutes   0.0.0.0:5432->5432/tcp, [::]:5432->5432/tcp



dev@dev-vm:~/Downloads/dba/rel/postgresql$ curl -I http://localhost
HTTP/1.1 302 FOUND
Server: gunicorn
Date: Thu, 16 Apr 2026 18:37:30 GMT
Connection: keep-alive
Content-Type: text/html; charset=utf-8
Content-Length: 213
Location: /login?next=/
Vary: Accept-Encoding
X-Frame-Options: SAMEORIGIN
Content-Security-Policy: default-src ws: http: data: blob: 'unsafe-inline' 'unsafe-eval';
X-Content-Type-Options: nosniff
X-XSS-Protection: 1; mode=block
Cross-Origin-Opener-Policy: same-origin
Set-Cookie: pga4_session=bfb18165-6056-4056-bb08-7ffe72fe0605!jFQGkOGSEDyscFSReMLid0wGF4rZ47QSn8WmrL51YZI=; Expires=Fri, 17 Apr 2026 18:37:30 GMT; HttpOnly; Path=/; SameSite=Lax
